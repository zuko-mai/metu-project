rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Simple example for this university project:
    // allow read/write only for authenticated users.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}